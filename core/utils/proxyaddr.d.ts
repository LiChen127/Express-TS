declare module 'proxy-addr' {
  // export default function proxyaddr(req: Request, list: string | string[]): string | false;
  // export function compile(list: string | string[]): Function;
  export function compile(list: any): Function;
}
